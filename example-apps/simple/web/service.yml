type: public
description: says hello to the world, ignores .txt files when file watching
author: exospheredev

messages:
  sends:
    - 'users.create'
  receives:
    - 'users.created'

development:
  scripts:
    run: node server.js
