name: Example application with broken setup script
description: For testing errors during setup
version: '1.0'

development:
  dependencies:
    - name: exocom
      version: 0.26.1

services:
  public:
    crasher:
      location: ./crasher
    runner:
      location: ./runner
