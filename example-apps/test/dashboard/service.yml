type: dashboard
description: shows a dashboard of the application
author: exospheredev

startup:
  online-text: online at port

messages:
  sends:
    - users.list
  receives:
    - users.listed
    - users.created

development:
  scripts:
    run: node_modules/exoservice/bin/exo-js
