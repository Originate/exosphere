version: "3"
services:
  frontend-service:
    build:
      context: ../../
      dockerfile: Dockerfile.dev
    command: bin/test
    container_name: frontend-service
    ports:
    - 8080:8080
    volumes:
    - ../../:/mnt
    environment:
      ROLE: frontend-service
