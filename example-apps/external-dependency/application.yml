name: external-dependency
description: For testing booting up dependencies
author: exospheredev
version: '1.0'

development:
  dependencies:
    - name: exocom
      version: 0.26.1
    - name: 'mongo'
      version: '3.4.0'
      config:
        volumes:
          - '{{EXO_DATA_PATH}}:/data/db'
        ports:
          - '27017:27017'

services:
  public:
    mongo:
      location: ./mongo
