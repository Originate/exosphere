name: simple app web server
description: An external web service that requires no setup

setup: echo 'setting up ... done'
startup:
  command: node server.js
  online-text: web service running 


messages:
  sends:
  receives:
