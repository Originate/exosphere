type: users-service
description: has some passing feature specs
author: exospheredev

startup:
  command: echo 'starting the users service'
  online-text: starting the users service

messages:
  receives:
  sends:

development:
  scripts:
    run: echo 'starting the users service'
    test: echo 'running tests for users service'
