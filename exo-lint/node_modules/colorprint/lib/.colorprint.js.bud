/**
 * Bud file for colorprint.js
 */

'use strict'

module.exports = {
  force: true,
  mode: '444',
  data: {
    levels: require('./.colorprint.json')
  }
}

if (!module.parent) {
  require('coz').render(__filename)
}