/**
 * Test case for fileHelper.
 * Runs with mocha.
 */
'use strict'

const fileHelper = require('../lib/helpers/file_helper.js')
const assert = require('assert')
const co = require('co')

describe('file-helper', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('File helper', () => co(function * () {

  }))
})

/* global describe, before, after, it */
