/**
 * Test case for writeToFile.
 * Runs with mocha.
 */
'use strict'

const writeToFile = require('../lib/file/write_to_file.js')
const assert = require('assert')
const co = require('co')

describe('write-to-file', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Write to file', () => co(function * () {

  }))
})

/* global describe, before, after, it */
