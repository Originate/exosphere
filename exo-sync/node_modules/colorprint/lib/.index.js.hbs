/**
 * {{pkg.description}}
 * @version {{pkg.version}}
 * @module {{pkg.name}}
 * @author {@link {{pkg.author.url}}|{{pkg.author.name}}{{{END_BRACE}}}
{{#each borrows}} * @borrows module:colorprint/lib~Colorprint#{{this}} as {{this}}
{{/each}} * @see {{pkg.homepage}}
{{#each pkg.dependencies}} * @requires {@link https://www.npmjs.org/package/{{@key}}|{{@key}}@{{{@this}}}{{{../END_BRACE}}}
{{/each}}
 */

'use strict'

const Colorprint = require('./colorprint')
const cliColor = require('cli-color')
const define = require('./define')
const create = require('./create')

let colorprint = create()
Object.assign(colorprint, {
 create,
 define,
 Colorprint,
 colors: cliColor
})

module.exports = colorprint
