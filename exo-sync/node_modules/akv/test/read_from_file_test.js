/**
 * Test case for readFromFile.
 * Runs with mocha.
 */
'use strict'

const readFromFile = require('../lib/file/read_from_file.js')
const assert = require('assert')
const co = require('co')

describe('read-from-file', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Read from file', () => co(function * () {

  }))
})

/* global describe, before, after, it */
