/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const filedel = require('./filedel')

let lib = filedel.bind(this)

Object.assign(lib, filedel, {
  filedel
})

module.exports = lib
