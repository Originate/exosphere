type: public
description: dummy html service used for testing setup only - does not run
author: test-author

dependency-data:
  exocom:
    receives:
      - todo.created
    sends:
      - todo.create

development:
  port: 80
  scripts:
    run: echo "does not run"
