type: public
description: says hello to the world, ignores .txt files when file watching
author: exospheredev

dependency-data:
  exocom:
    sends:
      - 'users.create'
    receives:
      - 'users.created'

development:
  port: 8080
  scripts:
    run: node server.js

production:
  port: 80
  health-check: '/'

remote:
  url: test-url.com
  cpu: 128
  memory: 128
