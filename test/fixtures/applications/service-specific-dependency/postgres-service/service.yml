type: worker
description: connects to a local instance of mongo
author: exospheredev

local:
  dependencies:
    postgres:
      type: 'postgres'
      image: 'postgres:9.6.2'
      config:
        external-in-production: true
        service-environment:
          DB_NAME: my_db

messages:
  sends:
  receives:
