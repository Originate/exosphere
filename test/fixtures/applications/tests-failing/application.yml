name: tests-failing

local:
  dependencies:
    exocom:
      image: originate/exocom:0.27.0

services:
  tweets-service:
    location: ./tweets-service
  users-service:
    location: ./users-service
