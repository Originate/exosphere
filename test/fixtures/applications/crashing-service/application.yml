name: crashing-service
description: Demonstrates basic Exosphere application startup
version: '1.0'

local:
  dependencies:
    - name: exocom
      version: 0.27.0

services:
  crasher:
    location: ./crasher
  runner:
    location: ./runner
