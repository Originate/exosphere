version: "3"
services:
  test-service:
    build:
      context: ${APP_PATH}/test-service
      dockerfile: Dockerfile.dev
    environment:
      EXOSPHERE_ENV: local
      ROLE: test-service
volumes: {}
