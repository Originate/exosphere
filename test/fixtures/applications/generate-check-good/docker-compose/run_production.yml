version: "3"
services:
  test-service:
    build:
      context: ${APP_PATH}/test-service
      dockerfile: Dockerfile.prod
    environment:
      EXOSPHERE_ENV: local
      ROLE: test-service
    restart: on-failure
volumes: {}
