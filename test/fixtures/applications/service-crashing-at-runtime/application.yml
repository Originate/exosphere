name: service-crashing-at-runtime
description: for testing behavior when a service crashes at runtime
version: '1.0'

local:
  dependencies:
    - name: exocom
      version: 0.27.0

services:
  crasher:
    location: ./crasher
  runner:
    location: ./runner
