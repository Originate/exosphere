name: external-dependency
description: For testing booting up dependencies
author: exospheredev
version: '1.0'

local:
  dependencies:
    - name: exocom
      version: 0.27.0

services:
  mongo:
    location: ./mongo
