name: test
description: Demonstrates basic Exosphere application startup
version: '1.0'

local:
  dependencies:
    - name: exocom
      version: 0.26.1

remote:
  dependencies:
    - name: exocom
      version: 0.26.1

services:
  web:
    location: ./web-server
  users:
    location: ./mongo-service
  dashboard:
    location: ./dashboard
