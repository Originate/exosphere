name: tests-passing
description: Allows to run application tests
version: 1.0

local:
  dependencies:
    - name: exocom
      version: 0.27.0

services:
  tweets-service:
    location: ./tweets-service
  users-service:
    location: ./users-service
