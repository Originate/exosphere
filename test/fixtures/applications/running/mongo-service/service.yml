type: worker
description: generic mongoDB storage service
author: exospheredev

messages:
  receives:
    - mongo.list
    - mongo.create
  sends:
    - mongo.listed
    - mongo.created

development:
  scripts:
    run: node server.js
