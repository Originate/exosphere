name: runtime crasher
description: for testing behavior when a service crashes at runtime
version: '1.0'

bus:
  type: exocom
  version: 0.21.1

services:
  public:
    crasher:
      location: ./crasher
    runner:
      location: ./runner
