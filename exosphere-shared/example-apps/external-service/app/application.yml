name: application with an external service
description: skeleton app using only an external service
version: '1.0.0'

dependencies:
  - type: exocom
    version: 0.21.8


services:
  public:
    web:
      location: ../web-service
