name: app-with-external-docker-images
description: An application that relies on dockerhub images
version: 0.0.1

services:
  public:
    local-service:
      location: ./local-service
    test:
      docker_image: originate/test-web-server
