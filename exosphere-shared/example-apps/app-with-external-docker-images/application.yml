name: app-with-external-docker-images
description: An application whose services are Dockerhub images
version: 0.0.0

bus:
  type: exocom
  version: 0.21.4

services:
  public:
    external-service:
      docker_image: originate/test-web-server
