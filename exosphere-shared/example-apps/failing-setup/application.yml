name: Example application with broken setup script
description: For testing errors during setup
version: '1.0'

dependencies:
  - type: exocom
    version: 0.22.1

services:
  public:
    crasher:
      location: ./crasher
    runner:
      location: ./runner
