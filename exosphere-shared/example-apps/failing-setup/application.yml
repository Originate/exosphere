name: Example application with broken setup script
description: For testing errors during setup
version: '1.0'

bus:
  type: exocom
  version: 0.21.2

services:
  public:
    crasher:
      location: ./crasher
    runner:
      location: ./runner
