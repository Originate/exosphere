#!/usr/bin/env bash

# install prod dependencies
go get -u github.com/satori/go.uuid

# install dev dependencies
go get -u github.com/alecthomas/gometalinter github.com/onsi/ginkgo/ginkgo github.com/onsi/gomega
gometalinter --install
